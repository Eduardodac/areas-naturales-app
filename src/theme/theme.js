import { createTheme } from "@mui/material";

const theme = createTheme({
    palette : {
        primary : {
            light : "#98ff96",
            main : "#009929",
            dark : "#003400",
            contrasText: "#ecfad8"
        }
    }
});

export default theme;